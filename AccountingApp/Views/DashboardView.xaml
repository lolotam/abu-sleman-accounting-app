<UserControl x:Class="AccountingApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccountingApp.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Summary Cards -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sales Card -->
            <materialDesign:Card Grid.Column="0" Margin="8" UniformCornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CurrencyUsd" Width="24" Height="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Sales" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding TotalSales, StringFormat='{}{0:C}'}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding SalesGrowth, StringFormat='{}{0:+0.0;-0.0;0.0}%'}" Foreground="{Binding SalesGrowthColor}" FontWeight="Medium"/>
                        <materialDesign:PackIcon Kind="{Binding SalesGrowthIcon}" Width="16" Height="16" Margin="4,0,0,0" Foreground="{Binding SalesGrowthColor}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Purchases Card -->
            <materialDesign:Card Grid.Column="1" Margin="8" UniformCornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CartOutline" Width="24" Height="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Purchases" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding TotalPurchases, StringFormat='{}{0:C}'}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding PurchasesGrowth, StringFormat='{}{0:+0.0;-0.0;0.0}%'}" Foreground="{Binding PurchasesGrowthColor}" FontWeight="Medium"/>
                        <materialDesign:PackIcon Kind="{Binding PurchasesGrowthIcon}" Width="16" Height="16" Margin="4,0,0,0" Foreground="{Binding PurchasesGrowthColor}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Expenses Card -->
            <materialDesign:Card Grid.Column="2" Margin="8" UniformCornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CashMinus" Width="24" Height="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Expenses" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding TotalExpenses, StringFormat='{}{0:C}'}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding ExpensesGrowth, StringFormat='{}{0:+0.0;-0.0;0.0}%'}" Foreground="{Binding ExpensesGrowthColor}" FontWeight="Medium"/>
                        <materialDesign:PackIcon Kind="{Binding ExpensesGrowthIcon}" Width="16" Height="16" Margin="4,0,0,0" Foreground="{Binding ExpensesGrowthColor}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Profit Card -->
            <materialDesign:Card Grid.Column="3" Margin="8" UniformCornerRadius="8" Background="{DynamicResource PrimaryHueLightBrush}">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChartLineVariant" Width="24" Height="24" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Net Profit" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="Medium" Foreground="White"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding NetProfit, StringFormat='{}{0:C}'}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0" Foreground="White"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding ProfitGrowth, StringFormat='{}{0:+0.0;-0.0;0.0}%'}" Foreground="White" FontWeight="Medium"/>
                        <materialDesign:PackIcon Kind="{Binding ProfitGrowthIcon}" Width="16" Height="16" Margin="4,0,0,0" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Charts Section -->
        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sales & Expenses Chart -->
            <materialDesign:Card Grid.Column="0" Margin="8" UniformCornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Sales & Expenses" FontWeight="Medium" Margin="0,0,0,16"/>
                    
                    <!-- Chart will be implemented with a chart control -->
                    <Border Grid.Row="1" Height="250" Background="#F5F5F5" CornerRadius="4">
                        <TextBlock Text="Sales & Expenses Chart" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5"/>
                    </Border>
                </Grid>
            </materialDesign:Card>

            <!-- Top Products Chart -->
            <materialDesign:Card Grid.Column="1" Margin="8" UniformCornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Top Products" FontWeight="Medium" Margin="0,0,0,16"/>
                    
                    <!-- Chart will be implemented with a chart control -->
                    <Border Grid.Row="1" Height="250" Background="#F5F5F5" CornerRadius="4">
                        <TextBlock Text="Top Products Chart" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5"/>
                    </Border>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Recent Transactions -->
        <materialDesign:Card Grid.Row="2" Margin="8,16,8,0" UniformCornerRadius="8">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Recent Transactions" FontWeight="Medium"/>
                    
                    <Button Grid.Column="1" Content="View All" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ViewAllTransactionsCommand}"/>
                </Grid>

                <DataGrid Grid.Row="1" Margin="0,16,0,0" 
                          ItemsSource="{Binding RecentTransactions}" 
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="None"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:d}'}" Width="100"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                        <DataGridTextColumn Header="Number" Binding="{Binding Number}" Width="120"/>
                        <DataGridTextColumn Header="Contact" Binding="{Binding ContactName}" Width="*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='{}{0:C}'}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Quick Actions -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Margin="8,0" Content="New Sale" Command="{Binding NewSaleCommand}" Style="{StaticResource ActionButton}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CartPlus" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="New Sale" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <Button Grid.Column="1" Margin="8,0" Content="New Purchase" Command="{Binding NewPurchaseCommand}" Style="{StaticResource ActionButton}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ShoppingCartPlus" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="New Purchase" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <Button Grid.Column="2" Margin="8,0" Content="New Expense" Command="{Binding NewExpenseCommand}" Style="{StaticResource ActionButton}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CashMinus" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="New Expense" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <Button Grid.Column="3" Margin="8,0" Content="Generate Reports" Command="{Binding GenerateReportsCommand}" Style="{StaticResource ActionButton}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileChart" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="Generate Reports" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>
    </Grid>
</UserControl>
